input: packages
output: .github/workflows/autogenerated-builder.yml

runs-on: ubuntu-20.04

overrides:
    hip-runtime-nvidia:
        build: false

    miopen-opencl:
        build: false

    openmp-extras:
        runs-on: self-hosted

    rccl:
        runs-on: self-hosted

    rocblas:
        runs-on: self-hosted

    rocfft:
        runs-on: self-hosted
